form = $('<%= escape_javascript(render "form", object: @attr = @attribute) %>');
content = $('.just-got-editted');
content.after(form)
content.css('display', 'none');
content.removeClass('just-got-editted')